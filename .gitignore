# Dependencies
node_modules/

# Build outputs
dist/
build/
coverage/
.nyc_output/

# Environment files
.env
.env.*
*.env
!.env.example
# ไฟล์ตัวอย่างต้อง commit ได้ (ค่าว่างล้วน มีแต่คำอธิบาย) ไม่งั้นคนที่ deploy
# จะไม่รู้ว่าต้องตั้ง env อะไรบ้าง
!.env.*.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Quasar / Vite
.quasar/
.vite/

# NestJS
*.tsbuildinfo

# Temporary files
tmp/
temp/
.cache/

# Python (News_AI_(Gemini) module) — 3 compiled files were committed on main before
# this rule existed (bd9c37d/d893ad6); this only stops new ones, doesn't retroactively
# remove those.
__pycache__/
*.pyc
